global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "token-tracker-api"
    static_configs:
      - targets: ["host.docker.internal:4000"]
      #For prod use -> [api:PORT] where PORT is the port your API is running on"]